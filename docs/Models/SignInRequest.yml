definitions:
  SignInRequest:
    type: object
    required: [email, password]
    properties:
      email:
        type: string
      password:
        type: string

components:
  schemas:
    SignInRequest:
      $ref: '#/definitions/SignInRequest'
