definitions:
  ErrorValidation:
    type: object
    required: [rule, field, message]
    properties:
      rule:
        type: string
      field:
        type: string
      message:
        type: string

components:
  schemas:
    ErrorValidation:
      $ref: '#/definitions/ErrorValidation'
